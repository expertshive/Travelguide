pluginManagement { includeBuild("../node_modules/@react-native/gradle-plugin") }
plugins { id("com.facebook.react.settings") }
extensions.configure(com.facebook.react.ReactSettingsExtension){ ex -> ex.autolinkLibrariesFromCommand() }
rootProject.name = 'TravelerGuideMobile'
include ':app'
includeBuild('../node_modules/@react-native/gradle-plugin')

// Gradle 9 removed RepositoryHandler.jcenter(). Some autolinked libraries still call it.
gradle.beforeProject { project ->
    def aliasJCenter = { repos ->
        repos.metaClass.jcenter = { Object[] args ->
            repos.mavenCentral()
        }
    }
    aliasJCenter(project.buildscript.repositories)
    aliasJCenter(project.repositories)
}
